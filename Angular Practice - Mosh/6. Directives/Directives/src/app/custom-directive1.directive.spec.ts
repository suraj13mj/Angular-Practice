import { CustomDirective1Directive } from './custom-directive1.directive';

describe('CustomDirective1Directive', () => {
  it('should create an instance', () => {
    const directive = new CustomDirective1Directive();
    expect(directive).toBeTruthy();
  });
});
